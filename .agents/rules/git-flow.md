# Git Workflow Rules

1. **Main Branch Restriction**: Agents are forbidden from suggesting pushes to the `main` branch.
2. **Branch Naming**: Every task must begin with checking out a new branch: `git checkout -b feat-[task-name]`.
3. **Conflict Risk Assessment**: Agents must provide a "Conflict Risk Assessment" before applying any code changes to ensure smooth integration without Git conflicts.
